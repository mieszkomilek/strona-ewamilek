name: Add PayPal to priced services once
on:
  push:
    paths:
      - '.github/scripts/add_paypal_to_prices_once.py'
      - '.github/workflows/add-paypal-to-prices-once.yml'
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install beautifulsoup4
      - run: python .github/scripts/add_paypal_to_prices_once.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add *.html styles.css
          git diff --cached --quiet || git commit -m "Add PayPal buttons beside all listed prices"
          git pull --rebase origin main
          git push origin HEAD:main
