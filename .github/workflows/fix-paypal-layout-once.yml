name: Fix PayPal layout once
on:
  push:
    paths:
      - '.github/scripts/fix_paypal_layout_once.py'
      - '.github/workflows/fix-paypal-layout-once.yml'
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install beautifulsoup4
      - run: python .github/scripts/fix_paypal_layout_once.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add *.html styles.css
          git diff --cached --quiet || git commit -m "Fix PayPal layout inside service cards"
          git pull --rebase origin main
          git push origin HEAD:main
